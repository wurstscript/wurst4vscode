.vscode/**
.github/**
.claude/**
src/**
out/**
node_modules/**
*.vsix
tsconfig*.json
webpack.config.js
.gitignore
.eslintrc*
# dist/ is in .gitignore so vsce would exclude it — explicitly re-include the built output
!dist/**
# source maps are only useful for local debugging, don't ship them
dist/**/*.map
